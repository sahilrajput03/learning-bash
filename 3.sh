#!/bin/bash
echo $UID
echo "If you run the script via 'sudo' prefixed you would see '0' value as that denotes if the program is initiaed as root access or not."
echo
echo "If the user executin the script has root permissions by default then wihout sudo the output would be '0'."
